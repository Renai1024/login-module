spring:
  security:
    filter:
      order: -100
    jwt:
      key: renaiskey
      expire: 7
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/testdatabase
    username: root
    password: root
